import React from 'react';
import EditResultForm from '../../components/EditResultForm';

const EditarResultadoLabScreen = ({ navigation, route }) => {
  return <EditResultForm navigation={navigation} route={route} />;
};

export default EditarResultadoLabScreen;