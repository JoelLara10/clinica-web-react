import React from 'react';
import UploadResultForm from '../../components/UploadResultForm';

export default function SubirResultadoScreen({ navigation, route }) {
  return <UploadResultForm navigation={navigation} route={route} />;
}