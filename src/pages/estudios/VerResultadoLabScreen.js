import React from 'react';
import ViewResultForm from '../../components/ViewResultForm';

const VerResultadoLabScreen = ({ navigation, route }) => {
  return <ViewResultForm navigation={navigation} route={route} />;
};

export default VerResultadoLabScreen;